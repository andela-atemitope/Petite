machine:
  ruby:
    version: 2.2.3
dependencies:
  pre:
    - gem install bundler -v '~> 1.10'
    - gem install sqlite3
    - gem install tilt
    - gem install pry
    - gem install pry-nav
  post:
    - rake
